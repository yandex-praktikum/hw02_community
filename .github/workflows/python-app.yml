name: Python application

on:
  push:
    branches: [ CI ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Git Clone Action
      uses: actions/checkout@v2
      with:
        repository: ${{ secrets.repository }}
        ssh-key: ${{ secrets.ACCESS_TOKEN }}
        path: yatube-old/
    - name: Lint with flake8 for author code
      run: |
        output=$(flake8 yatube-old/ "$1")
        status="$?"
        if [ $status ]; then echo "::set-output name=flake8_output::$output"; fi
        echo $flake8_output
        echo $status
    - name: Lint with flake8
      run: |
        mv yatube-old/yatube/ ./
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --max-complexity=10 --max-line-length=127 --statistics
#     - name: Create an issue
#       uses: actions-ecosystem/action-create-issue@v1
#       with:
#         github_token: ${{ secrets.ACCESS_TOKEN }}
#         title: ${{ steps.date.outputs.today }}
#         body: |
#           ## This week's TODO
#           - [ ] Have a meeting with UX team
    - name: Test with pytest
      env:
        SECRET_KEY: "5UP3R-53CR3T-K3Y-FR0M-TurboKach"
        DJANGO_SETTINGS_MODULE: yatube.settings
        DEBUG: 1
        ALLOWED_HOSTS: "*"
      run: |
        py.test
